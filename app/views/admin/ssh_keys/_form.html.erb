<%= simple_form_for([:admin, user, ssh_key]) do |form| %>
    <%= form.input(:name) %>
    <%= form.input(:key, :input_html => { :rows => 15 }) %>

    <%= form.button(:submit) %> or
    <%= link_to_cancel(admin_user_ssh_keys_path(user)) %>
<% end %>




