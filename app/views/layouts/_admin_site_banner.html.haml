%div.navbar.navbar-inverse
  %div.navbar-inner
    %div.container-fluid
      = link_to("Code@Berkeley &nbsp;<span class='label label-important'> Admin</span>".html_safe, admin_root_url, :class => 'brand')
      %div.nav-collapse.collapse.pull-right
        %ul.nav
          %li.dropdown
            %a.dropdown-toggle{ 'data-toggle' => 'dropdown', :href => '#menu1' }
              = current_user.full_name
              %span.caret
            %ul.dropdown-menu
              %li= link_to('Profile', edit_profile_url)
              %li= link_to('SSH Public Keys', ssh_keys_url)
              %li= link_to('Repositories', repositories_url)
              %li= link_to('Spaces', spaces_url)

            %li= link_to('Dashboard', dashboard_url)
            %li= link_to('Help', help_url)
            %li= link_to('Log Out', logout_url, :confirm => "Click 'OK' to confirm logout")
